# MeshAPI gateway (https://developers.meshapi.ai) — dashboard > API Keys
MESH_API_KEY=rsk_your_meshapi_key_here
MESHAPI_BASE_URL=https://api.meshapi.ai
# provider/model string routed through MeshAPI, e.g. openai/gpt-4o-mini or mistral/mistral-large-3-675b-instruct
MESHAPI_CHAT_MODEL=openai/gpt-4o-mini

# RAG (native_rag_app -- retrieval runs entirely through MeshAPI's managed RAG store,
# no separate vector database or embeddings config needed)
RAG_TOP_K=3

# Pinecone (https://www.pinecone.io — dashboard > API Keys) -- only needed for
# experiments/02_rag_multiagent(_lazy_imports).ipynb, which builds its own vector store.
# Not used by native_rag_app.
PINECONE_API_KEY=your_pinecone_key_here

# native_rag_app voice (optional overrides -- defaults shown work out of the box)
MESHAPI_TTS_MODEL=hexgrad/kokoro-82m
MESHAPI_TTS_VOICE=af_heart
MESHAPI_STT_MODEL=elevenlabs/scribe_v1
